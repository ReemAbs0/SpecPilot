# SpecPilot frontend environment variables.
# Copy this file to `.env` and adjust as needed. `.env` is git-ignored.

# Base URL of the backend API. In development the Vite dev server proxies /api requests
# to this target (see vite.config.ts and quickstart.md).
VITE_API_BASE_URL=http://localhost:4000
